SPARKY - HELPFUL ON ALL TOPICS

Replace these files in your existing GitHub project:
- index.html
- api/sparky.js

This version:
- builds on the student's own idea instead of replacing it with a preset list
- handles natural directions such as saints, family members, sportspeople, First Nations stories, animals, personal experiences and more
- works across all 7 competition topics
- keeps secure Gemini via /api/sparky
- keeps the local fallback if Gemini is unavailable

Vercel still needs:
GEMINI_API_KEY

No new Supabase SQL is required.

After upload, wait for Vercel to show a new Ready/Production deployment, then hard-refresh the site.
