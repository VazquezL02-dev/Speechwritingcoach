SPARKY - CLASSROOM BUSY FIX

Replace ONLY:
api/sparky.js

Do not change:
- index.html
- Supabase / SQL
- Vercel environment variables

This version:
- uses gemini-3.5-flash-lite
- is better suited to high-volume, low-latency classroom use
- sends less conversation history per request
- handles 429 "busy" errors more cleanly

After upload:
1. Commit api/sparky.js in GitHub.
2. Wait for Vercel Ready / Production.
3. Hard-refresh the student iPad.
4. Test Sparky again.

If many students use Sparky simultaneously, your Google API project's quota can still be reached. If that happens consistently, the long-term fix is increasing the Gemini API project tier/quota.
